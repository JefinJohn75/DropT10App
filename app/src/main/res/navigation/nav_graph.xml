<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/scanFragment">

    <!--
        ScanFragment is the only screen for now.
        When you add ConnectFragment later, add it here
        with an action from scanFragment → connectFragment.
    -->
    <fragment
        android:id="@+id/scanFragment"
        android:name="com.example.blescanner.ScanFragment"
        android:label="BLE Scanner" />

</navigation>